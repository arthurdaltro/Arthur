{
  "name": "btc-start-prototype",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Concept prototype: restructured /start intake for Bethel Transformation Center (not affiliated).",
  "scripts": {
    "start": "node execution/server.mjs",
    "test": "node --test tests/*.test.mjs",
    "probe": "node execution/probe_handoff.mjs && node execution/probe_sheets.mjs && node execution/probe_resend.mjs"
  },
  "engines": { "node": ">=20" }
}
